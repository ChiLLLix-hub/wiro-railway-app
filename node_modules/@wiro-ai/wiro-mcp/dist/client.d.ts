import type { ListTasksParams, SearchModelsParams, RunModelResult, TaskDetailResponse, TaskListResponse, ToolListResponse, ToolDetailResponse, ExploreResponse, FileUploadResponse, Task } from './types.js';
export type { ListTasksParams, SearchModelsParams, RunModelResult, TaskDetailResponse, TaskListResponse, ToolListResponse, ToolDetailResponse, ExploreResponse, FileUploadResponse, } from './types.js';
export type { Task, TaskCustomToolCallSegment, TaskFinishReason, TaskFunctionCallSegment, TaskOutput, TaskOutputRawContent, TaskSegment, TaskTextSegment, TaskToolCallSegment, TaskToolCallStatus, TaskUsage, ToolListItem, ToolParameterGroup, ToolParameterItem, } from './types.js';
export interface TaskReference {
    tasktoken?: string;
    taskid?: string;
}
export interface WaitForTaskOptions {
    signal?: AbortSignal;
    onPoll?: (task: Task, context: {
        attempt: number;
        elapsedMs: number;
    }) => void | Promise<void>;
    pollIntervalMs?: number;
    maxConsecutiveErrors?: number;
}
export interface TrustedProxyContext {
    clientIp: string;
    sharedSecret: string;
}
export interface WiroClientOptions {
    trustedProxy?: TrustedProxyContext;
}
export declare class WiroApiError extends Error {
    readonly status: number;
    readonly responseBody: string;
    constructor(status: number, responseBody: string);
}
export declare class TaskWaitTimeoutError extends Error {
    readonly timeoutMs: number;
    readonly lastDetail?: TaskDetailResponse;
    readonly lastError?: unknown;
    constructor(timeoutMs: number, lastDetail?: TaskDetailResponse, lastError?: unknown);
}
export declare class TaskPollingError extends Error {
    readonly lastDetail?: TaskDetailResponse;
    readonly lastError?: unknown;
    constructor(message: string, lastDetail?: TaskDetailResponse, lastError?: unknown);
}
export declare class WiroClient {
    private readonly apiKey;
    private readonly apiSecret?;
    private readonly baseUrl;
    private readonly trustedProxy?;
    readonly authType: 'signature' | 'apikey-only';
    constructor(apiKey: string, apiSecret?: string, baseUrl?: string, options?: WiroClientOptions);
    private getAuthHeaders;
    private request;
    searchModels(params?: SearchModelsParams): Promise<ToolListResponse>;
    explore(): Promise<ExploreResponse>;
    getModelSchema(model: string): Promise<ToolDetailResponse>;
    runModel(model: string, params: Record<string, unknown>, signal?: AbortSignal): Promise<RunModelResult>;
    getTask(opts: TaskReference, signal?: AbortSignal): Promise<TaskDetailResponse>;
    listTasks(params?: ListTasksParams, signal?: AbortSignal): Promise<TaskListResponse>;
    cancelTask(task: string | TaskReference, signal?: AbortSignal): Promise<Record<string, unknown>>;
    killTask(task: string | TaskReference, signal?: AbortSignal): Promise<Record<string, unknown>>;
    uploadFile(fileUrl: string, fileName?: string): Promise<FileUploadResponse>;
    waitForTask(task: string | TaskReference, timeoutMs?: number, options?: WaitForTaskOptions): Promise<TaskDetailResponse>;
}
//# sourceMappingURL=client.d.ts.map